import Typography from "typography";

const typography = new Typography({
  baseFontSize: "18px",
  baseLineHeight: 1.666,
  headerFontFamily: ["Helvetica", "Segoe UI", "Ubuntu", "Arial", "sans-serif"],
  bodyFontFamily: ["Helvetica", "Segoe UI", "Ubuntu", "Arial"],
});

export default typography;
